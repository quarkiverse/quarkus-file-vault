---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "File Vault"
metadata:
  keywords:
    - "vault"
    - "keystore"
    - "secret"
    - "password"
  categories:
    - "security"
  status: "preview"
  config:
    - "quarkus.file.vault."
 
